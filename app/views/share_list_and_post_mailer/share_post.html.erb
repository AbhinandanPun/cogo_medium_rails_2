<!DOCTYPE html>
<html>
<head>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
</head>
<body>
  <h1>Shared Post</h1>
  <p>Hello <%= @recipient_email %>,</p>
  <p>You have received a shared post from <%= @current_user.username %>.</p>
  <h2><%= @post.title %></h2>
  <p>Topic: <%= @post.topic %></p>
  <p><%= @post.text %></p>
  <p>Read Time: <%= @post.read_time %> minutes</p>
  <p>Like Count: <%= @post.like_count %></p>
  <p>Comment Count: <%= @post.comment_count %></p>
  <p>View Count: <%= @post.view_count %></p>
  <p>File URL: <a href="http://127.0.0.1:3000/posts/+#{@post.id}"></a> see post</p>
  <% if !@comments.nil? %>
    <h3>Comments:</h3>
    <ul>
      <% @comments.each do |comment| %>
        <li><%= comment.body %></li>
      <% end %>
    </ul>
  <% else %>
    <p>No comments yet.</p>
  <% end %>

  <p>Thank you!</p>
</body>
</html>